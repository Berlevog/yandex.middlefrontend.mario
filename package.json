{
  "name": "yandex.middlefrontend.mario",
  "version": "1.0.0",
  "description": "",
  "private": true,
  "scripts": {
    "build:client": "npm run build -w @mario/client",
    "build:server": "npm run build -w @mario/server",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "workspaces": [
    "packages/client",
    "packages/server"
  ]
}
